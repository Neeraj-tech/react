edit config file and move to server folder
under client folder run following command:
  npm install
  npm start
under server folder run following command:
  npm install
  node .

requires:
  mongoDB 
